#include "alarms.h"
#include <EEPROM.h>

void setAlarm(int hour, int minute) {
  // Logique pour programmer une alarme
}

void checkAlarms() {
  // Logique pour vérifier et déclencher les alarmes
}